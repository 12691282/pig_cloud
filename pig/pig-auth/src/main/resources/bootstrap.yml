spring:
  application:
    name: pig-auth
  profiles:
    active: dev
  cloud:
    config:
      fail-fast: true
      discovery:
        service-id: pig-config-server
        enabled: true
      profile: ${spring.profiles.active}
      label: ${spring.profiles.active}
---
spring:
  profiles: dev
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/pig_data?autoReconnect=true&characterEncoding=utf8
    username: root
    password: root
eureka:
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 20
  client:
    serviceUrl:
      defaultZone: http://pig:gip6666@localhost:1025/eureka
    registry-fetch-interval-seconds: 10

---
spring:
  profiles: prd
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://pig:gip6666@eureka:1025/eureka
